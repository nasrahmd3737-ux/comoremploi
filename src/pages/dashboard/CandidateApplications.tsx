import CandidateDashboard from "./CandidateDashboard";

// Reuse the same component — the dashboard already shows all applications
export default function CandidateApplications() {
  return <CandidateDashboard />;
}
